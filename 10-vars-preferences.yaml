- name: variables preferences
  hosts: frontend
  # vars: 
  #   full_name: "Chinnu from play"
  # vars_files:
  # - course.yaml
  # vars_prompt:
  # - name: Please enter your full name 
  #   private: false
  tasks:
    - name: print vars preferences
      vars:
        #full_name: "Chinnu from TASK level"
      debug:
        msg: "Hi, {{ full_name}}"


# 1. CMD args
# 2. TASK level
# 3. Vars_files
# 4. vars_prompt
# 5. Play 